# 规范 PDF 原件(约 575MB):用户资料,不入版本库;按 config.json 的 library_dir 提供
guifansrc/

# 可再生索引数据:重跑 python spec.py index 即可重建(体积大)
library_data/

# 第三方语言包 chi_sim(约 43MB):可从 tessdata_best 重新下载,
# 或运行 spec.py 时按 config.json 的 ocr_tessdata_dir 恢复
tools/guifan-chaxun-scripts/scripts/tessdata/

# Python
__pycache__/
*.pyc

# 提交前审查标记(本地状态,每次刷新变化,不入库)
.claude/review-stamp
